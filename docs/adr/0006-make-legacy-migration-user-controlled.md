# Make legacy migration user-controlled

KlotskiPuzzle v2 detects legacy profiles and saves and offers a one-time user choice to import, skip, or explicitly delete them; it never removes another user's data silently. Legacy leaderboard entries cannot become Qualified Results because they lack the puzzle, movement-rule, assistance, and rules-version evidence required by the new contract.
