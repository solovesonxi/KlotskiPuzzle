# Export read-only HTML Experiment Reports

KlotskiPuzzle may render a self-contained HTML Experiment Report from an Experiment Record so results can be viewed through GitHub Pages, blogs, or a local browser without installing Java. The report remains a read-only presentation layer and never reimplements search or movement rules in JavaScript, preserving Java 22 as the single source of solver behavior.
