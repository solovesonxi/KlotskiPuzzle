# Use local profiles without passwords

KlotskiPuzzle uses optional local Player Profiles rather than password-based accounts. Because saves and results are editable files owned by the current operating-system user, password hashing cannot provide trustworthy identity or anti-cheat guarantees; removing authentication reduces first-run friction and keeps Lab Mode independent of player data.
